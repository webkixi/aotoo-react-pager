import Pager, { page } from './page';

const nav = Pager.nav
const getallPages = Pager.getAllPages
const pages = Pager.pages

export {
  nav, 
  getallPages, 
  pages, 
  page
}

export default Pager
